{% extends 'base.html.twig' %}

{% block title %}Village Green - Rubrique{% endblock %}

{% block body %}

    <div class="d-flex justify-content-center">
        <h2 class="mt-3">Catégories</h2>
    </div>

    <div class="d-flex container justify-content-center">
        <div class="row wrap">

            {% for rubrique in rubriques %}
                <div class="col-4">
                    <a href="{{ path('app_sousrubriqueId', {id: rubrique.id}) }}" class="card m-2">
                        <div>
                            <img src="{{ asset('upload/product/600x350.jpeg') }}" class="img-fluid" alt="img_test">
                            <h4 class="d-flex card-title justify-content-center">{{ rubrique.NomRubrique }}</h4>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    </div>
    
{% endblock %}
