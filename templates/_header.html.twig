{% block header %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_home')}}">Village Green</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                </li>
                <li class="nav-item">
                <a class="nav-link" href="{{ path('app_rubrique')}}">Rubriques</a>
                </li>
                {# <li class="nav-item">
                <a class="nav-link" href="{{ path('app_sousrubrique')}}">Sous-rubriques</a>
                </li> #}
                {# <li class="nav-item">
                <a class="nav-link" href="{{ path('app_product')}}">Produits
                </a> #}
            </ul>

            <form class="d-flex">
                {# <input class="form-control me-sm-2" type="search" placeholder="Search">
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button> #}


                {# todo : remplacer le bouton par un bouton deconnexion en cas de session #}
                {% if app.user %}
                    <div class="nav-item">
                        <a class="btn btn-success m-1" href="{{ path('app_logout') }}">Deconnexion</a>
                    </div>
                {% else %}
                    <div class="nav-item">
                        <a class="btn btn-success m-1" href="{{ path('app_login') }}">Connexion</a>
                    </div>
                {% endif %}
                

                <div class="nav-item">
                    <a class="btn btn-success m-1" href="{{ path('app_register') }}">S'inscrire</a>
                </div>
                
                <div class="nav-item">
                    <a class="btn btn-sucess m-1" href="{{ path('app_cart') }}">
                        <i class="fa fa-shopping-cart" style='font-size:24px'></i>
                    </a>
                </div>

                {# <p class="card-text">Nombre de produit(s) commandé(s) : {{ totalPro }}</p> #}

            </form>

            </div>
        </div>
    </nav>
{% endblock %}