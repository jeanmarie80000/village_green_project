{% extends 'base.html.twig' %}

{% block title %}Village Green - Rubrique{% endblock %}

{% block body %}

    <div class="d-flex justify-content-center">
        <h2 class="m-3 ml-0">Catégories</h2>
    </div>

    <div class="d-flex container justify-content-center">
        <div class="row wrap">

            {% for rubriques in rubrique %}
                <div class="col-6">
                    <a href="{{ path('app_sousrubrique')}}"  class="card m-2" style='width: 30rem;'>
                        <div>
                            <img src="{{ asset('upload/product/500x350.jpeg') }}" class="img-fluid" alt="img_test">
                            <h4 class="card-title">{{ rubriques.CodeRubrique }}</h4>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    </div>
    
{% endblock %}
