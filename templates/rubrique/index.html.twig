{% extends 'base.html.twig' %}

{% block title %}Village Green - Rubrique{% endblock %}

{% block body %}

    <div class="d-flex justify-content-center">
        <h1 class="catTitle mt-4">Catégories</h1>
    </div>

    <div class="d-flex container justify-content-center">
        <div class="row wrap">

            {% for rubrique in rubriques %}
                <div class="col-6">
                    <a href="{{ path('app_sousrubriqueId', {id: rubrique.id}) }}" class="card m-2 imgTitle">
                        <div>
                            <img src="{{ asset('upload/product/600x350.jpeg') }}" class="img-fluid imgCat" alt="img_rubrique">
                            <h4 class="d-flex card-title justify-content-center imgTitle">{{ rubrique.NomRubrique }}</h4>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    </div>
    
{% endblock %}
